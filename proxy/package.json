{
  "name": "flight-agent-proxy",
  "version": "1.0.0",
  "description": "Local proxy to forward Claude API requests (bypasses browser CORS)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}
